{
  "name": "jd-survey",
  "version": "0.2.0",
  "description": "Filesystem survey plugin for Obsidian (ports the 08.41 survey protocol).",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
